<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon3"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.TripMaster">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".BudgetPlace"
            android:parentActivityName=".MainActivity"
            android:label="Budget App - Place"/>
        <activity android:name=".BudgetCinema"
            android:parentActivityName=".MainActivity"
            android:label="Budget App - Cinema"/>
        <activity android:name=".BudgetFood"
            android:parentActivityName=".MainActivity"
            android:label="Budget App - Menu"/>
        <activity android:name=".Map" />
        <activity android:name=".Feedback"
            android:label="Budget App - Feedbck"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".Budget"
            android:label="Budget App - Result"
            android:parentActivityName=".MainActivity"/>


        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".Navi" />
        <activity android:name=".Hotel" />
        <activity android:name=".Weather" />
        <activity android:name=".Destination" />
        <activity android:name=".User_Reviews" />
        <activity android:name=".HotelDetails" />
        <activity android:name=".Adventure" />
        <activity android:name=".Beach" />
        <activity android:name=".Category" />
        <activity android:name=".City" />
        <activity android:name=".DestinationDetails" />
        <activity android:name=".HotelReviews" />
        <activity android:name=".Park" />
        <activity android:name=".Route" />
        <activity android:name=".ScubaDiving" />
        <activity android:name=".Tourist" />
        <activity android:name=".View_user_reviews" />
        <activity android:name=".Trekking" />
        <activity android:name=".MainActivity"
            android:label="Budget App - Home"/>
    </application>

</manifest>